# Core APIs and Async Handling
fastapi==0.140.0
uvicorn[standard]==0.51.0
aiohttp==3.14.3
httpx==0.27.0

# Crypto Exchange Integration
# ccxt 4.5.68 currently hard-pins cryptography 49.0.0. The corresponding
# audit exception is tracked in the repository CI and must be removed as soon
# as upstream releases a compatible version.
ccxt==4.5.68

# Data Processing
pydantic==2.13.4
pydantic-settings==2.14.2

# Observability and Utilities
prometheus-client==0.20.0
loguru==0.7.2

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
