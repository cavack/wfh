# Core APIs and Async Handling
fastapi==0.140.0
uvicorn[standard]==0.51.0
# ccxt 4.5.68 hard-pins aiohttp 3.14.1. Keep the direct dependency explicit
# until upstream allows 3.14.3+; the exact audit exceptions are tracked in CI.
aiohttp==3.14.3
httpx==0.28.1

# Crypto Exchange Integration
# ccxt 4.5.68 also hard-pins cryptography 49.0.0. The corresponding audit
# exception is tracked in CI and must be removed as soon as upstream releases
# compatible dependency pins.
ccxt==4.5.68

# Data Processing
pydantic==2.13.4
pydantic-settings==2.14.2

# Observability and Utilities
prometheus-client==0.26.0
loguru==0.7.2

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
