route:
  receiver: telegram-webhook
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: telegram-webhook
    webhook_configs:
      - url: http://172.23.0.2:8080/alerts
        send_resolved: true
